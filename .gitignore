trading-os.zip

# Bases de dados locais (dados reais / sensíveis)
*.db

# Arquivos pesados
*.tar.gz

# Variáveis de ambiente locais (chaves de API)
.env*.local

# Credenciais Git (token)
.git-credentials.env

# Lockfile do Bun (projeto padronizado em NPM)
bun.lock

# Backups locais da base de dados (nunca versionar)
backups/
